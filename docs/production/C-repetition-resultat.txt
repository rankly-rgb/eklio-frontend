RÉPÉTITION À BLANC DE LA MISE EN PRODUCTION — 2026-09-26
════════════════════════════════════════════════════════
0 échec sur les étapes jouables.

Rejouée depuis zéro : la copie de production est reconstruite à chaque
lancement depuis les 133 migrations que `main` porte, puis les 24 nouvelles
sont appliquées dans l'ordre.

⚠ DEUX DÉFAUTS DE LA RÉPÉTITION ELLE-MÊME, CORRIGÉS CE JOUR
  · Les chemins étaient relatifs. `scripts/local-verify-stub-schema.sql` et
    `supabase/migrations/` vivent dans eklio-backend ; lancée depuis
    eklio-frontend, la répétition rendait 31 échecs dont AUCUN n'était un
    défaut de migration — l'amorce n'avait pas été trouvée. Une répétition de
    mise en production qui ne tourne que depuis un répertoire non écrit
    quelque part est une répétition qu'on rate le jour où on en a besoin.
  · La base n'était pas reconstruite. Elle restait dans l'état où la
    répétition précédente l'avait mise, donc l'étape 3 n'appliquait plus rien
    et annonçait « les 21 appliquées » en n'en appliquant zéro.
  · Le compte de migrations était un littéral (155). Il se dérive du dépôt.

ÉTAPES
  −1  copie de production reconstruite      133 migrations rejouées
   0  correctif de restauration seul        appliqué
   1  rejeu complet sur base neuve          157 migrations, 0 échec
   2  sauvegarde et restauration            69 tables, section_types 11 = 11
   3  les 24 nouvelles, dans l'ordre        appliquées
   4  F12 : la matrice refuse à vide        240 lignes, 0 État vendable
  7b  les deux tables de F17                présentes
   8  les cinq fonctions de banque          présentes et répondent
  8b  crédit : chokepoint, vue, invariant   présents
  8d  le juge (hors base)                   voir B2
   9  la licence                            3 colonnes, 2 contraintes, matrice 240
  10  F38                                   immédiate passe, à distance bloque
  11  18 motifs + 5 gâchettes armées        toutes
  12  libération des assignations           délai de grâce 3 h, éprouvé des 2 côtés
  13  ce qui ne se joue pas en base         portillon, crise, mention de licence

PROCÉDURE DE RESTAURATION, VÉRIFIÉE À PART
  86 tables identiques ligne à ligne, contenu compris — 0 écart.
  Et elle a retiré une contrainte QUI N'EXISTAIT PAS à son écriture
  (`content_items_payload_valid`), parce qu'elle énumère depuis le catalogue
  au lieu de porter une liste.

CE QUI NE PEUT PAS ÊTRE JOUÉ À BLANC
  · l'étape 5 (Vercel), l'étape 6 (DNS), l'étape 7 (Stripe en mode réel) :
    aucun objet local, et le brief interdit d'y toucher.
  · le chemin PRODUIT de génération (condition 3 de B3) : le garde-fou de
    banque et la libération des assignations vivent dans le harnais, pas dans
    la route. Ce trou est le même depuis l'origine et il porte maintenant deux
    choses.
  · un mois réel regardé par une personne (étape 10) : quinze minutes, et
    elles n'ont pas eu lieu.
